# Meridian — настольный клиент

Клиент Meridian VPN для macOS (Apple Silicon) и Windows.

Это репозиторий **только для выпуска готовых сборок** — здесь нет
исходного кода. Скачивайте файлы со страницы
[Releases](../../releases), исходники и история разработки — в закрытом
репозитории проекта.

## Установка

### macOS (Apple Silicon)

1. На странице [Releases](../../releases) скачайте файл для macOS
   (`meridian-*-darwin-arm64.dmg`) из последнего выпуска.
2. Откройте образ, перетащите Meridian VPN в «Программы». Сборка
   подписана сертификатом Developer ID Application и нотаризована
   Apple — предупреждение Gatekeeper при первом запуске появляться не
   должно.
3. Приложению для работы туннеля потребуется системная служба с правами
   администратора — при первом запуске macOS попросит её разрешить
   (Системные настройки → Основные → Объекты входа и расширения →
   «Разрешить в фоне»).

### Windows (amd64)

1. На странице [Releases](../../releases) скачайте файл для Windows
   (`meridian-*-windows-amd64.*`) из последнего выпуска.
2. При запуске Windows SmartScreen может показать «Windows защитила ваш
   компьютер» — это ожидаемо для сборки без подписи кода (подпись
   появится позже, см. раздел ниже). Чтобы всё равно запустить:
   нажмите **«Подробнее»**, затем **«Выполнить в любом случае»**.
3. Приложению для работы туннеля нужны права администратора — Windows
   запросит подтверждение (UAC) при запуске.

### Проверка целостности файла (необязательно, для тех, кто хочет свериться)

Каждый выпуск содержит файл `SHA256SUMS` с контрольными суммами всех
файлов сборки. Проверить скачанный файл:

```
# macOS / Linux
shasum -a 256 -c SHA256SUMS

# Windows (PowerShell)
Get-FileHash meridian-*-windows-amd64.exe -Algorithm SHA256
# сравнить вывод со строкой в SHA256SUMS вручную
```

## Про предупреждение SmartScreen (Windows)

Сборка для Windows пока не подписана сертификатом подписи кода — это
отдельный, дорогостоящий шаг, ещё не сделан. Предупреждение SmartScreen
означает «эта программа не от известного разработчика», а не «эта
программа опасна» — но проверяйте, что скачиваете файл именно с этой
страницы (`github.com/JinComputers/meridian-desktop-releases`), не с
зеркал и не по чужим ссылкам. Подпись появится по мере готовности
проекта.

Сборка для macOS подписана сертификатом Developer ID Application и
нотаризована Apple — Gatekeeper эту сборку уже проверил и предупреждать
не должен.

## Что это за клиент

Meridian — VPN-клиент. Настольная версия (это приложение) — для macOS и
Windows, использует то же ядро подключения, что и мобильные приложения
проекта.

## Проблемы и обратная связь

Этот репозиторий — только для файлов выпуска, issues и обсуждения здесь
не ведутся. Если что-то не работает — сообщите тем же способом, каким
вы обычно на связи с проектом.
